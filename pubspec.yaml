name: drop_shadow
description: Drop shadow effect for any widget in Flutter
version: 0.0.1
homepage: https://github.com/iisprey/drop_shadow

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.0

flutter: