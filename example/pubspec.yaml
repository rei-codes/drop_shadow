name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  drop_shadow:
    path: ../

dev_dependencies:
  flutter_lints: ^1.0.0

flutter: null